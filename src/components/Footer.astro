---
const currentYear = new Date().getFullYear();

const navLinks = [
	{ href: '/', label: 'Home' },
	{ href: '/about', label: 'About' },
	{ href: '/services', label: 'Services' },
	{ href: '/contact', label: 'Contact' },
];
---

<footer class="bg-dark px-4 py-12 sm:px-8">
	<div class="mx-auto max-w-6xl">
		<div class="flex flex-col items-center justify-between gap-6 md:flex-row">
			<a href="/" class="text-xl font-bold text-white">Braj<span class="text-accent-teal">Go</span></a>
			<nav class="flex flex-wrap justify-center gap-8 text-sm">
				{navLinks.map((link) => (
					<a href={link.href} class="text-mint/80 transition hover:text-white">{link.label}</a>
				))}
			</nav>
		</div>
		<div class="mt-10 border-t border-white/10 pt-8 text-center text-sm text-mint/60">
			© {currentYear} Braj Go Tour and Travels. All rights reserved.
		</div>
	</div>
</footer>
